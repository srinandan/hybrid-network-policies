apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: netpol-ad
  namespace: apigee-system
spec:
  podSelector:
    matchLabels:
      app: apigee-deployment-admissionhook
  policyTypes:
    - Ingress
  ingress:
  - from: []
    ports:
    - protocol: TCP
      port: 8443
---
